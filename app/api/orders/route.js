export async function POST(request) { try { const body = await request.json(); if (!body.name || !body.email || !body.phone || !body.cakeType) { return new Response(JSON.stringify({ error: 'Missing required fields' }), { status: 400 }); } const order = { id: Date.now(), ...body, createdAt: new Date().toISOString(), status: 'pending' }; console.log('Order received:', order); return new Response(JSON.stringify({ success: true, order }), { status: 201, headers: { 'Content-Type': 'application/json' } }); } catch (error) { return new Response(JSON.stringify({ error: 'Failed to create order' }), { status: 500 }); } } export async function GET() { return new Response(JSON.stringify({ message: 'Order API endpoint' }), { status: 200, headers: { 'Content-Type': 'application/json' } }); }